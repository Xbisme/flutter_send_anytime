# Safe Send — Changelog

> Append-only ship history. One entry per spec as it lands. Newest at top.
> Format: `### YYYY-MM-DD — Spec #NNN <Name> ✅ <verb>` + 1–4 bullets.

### 2026-06-25 — Spec #007 QR Connect ✅ IMPLEMENTED

- **QR pairing shipped** (second connection method, reusing the #003 rendezvous unchanged): the **sender** gets a **QR tab** in the Connect hub that renders the *live* pairing code as a `safesend://connect?v=1&code=NNNNNN` QR — a second presentation of the **same hosting session** (switching Mã 6 số ↔ QR never regenerates the code or opens a second socket), with the readable 6-digit code beneath and a **screen-brightness boost** while shown. The **receiver** gets a dedicated full-screen **scanner page** (`AppRoutes.qrScan`) reached from a "Quét mã QR" button — camera scan + **torch** + **pick-from-photo** (reusing `file_picker` → `analyzeImage`, no new photo permission); a valid scan auto-`joinWithCode`s straight into the #005 accept/reject prompt. Foreign/expired QR → gentle toast, keeps scanning (never dead-ends). Home "Quét QR" lands straight on the scanner. QR-paired transfers record `pairingMethod = qr` (enum already reserved — **no schema change**).
- **First runtime permission** (camera): `permission_handler` behind a core `CameraPermissionService` seam → granted/denied/permanentlyDenied/restricted handled distinctly; blocked camera shows **Open Settings** + pick-from-photo fallback (no dead preview). iOS `NSCameraUsageDescription` (VI+EN) + Podfile `PERMISSION_CAMERA=1` macro added (first `pod install` will churn `Podfile.lock`, deferred to device build).
- **Architecture**: pure extension of `features/pairing/` + two additive core seams — a shared `ConnectLink` codec in `core/domain/pairing/` (build/parse, reuses `SignalingProtocol.isValidCode`, **deep-link-ready for #008**) and additive `ConnectRequest.openScanner` / `ConnectResult.method` core fields. `pairingMethod` threads ConnectResult → transfer cubits → the #006 mappers (was hardcoded `sixDigitCode`). The scanner page stays signaling-free (returns a code String; the receiver panel calls the existing join). **No engine/signaling/transport edits.** The QR tab is sender-only (receiver hides the QR segment — clarified).
- **Quality**: `dart analyze lib test` = **0 issues**; `flutter test` = **199 passed** (167 prior + 32 new: ConnectLink acceptance matrix, QrScanCubit permission/latch/foreign/torch, scanner blocked-permission page, sender QR-panel render + tab-switch invariance, receiver scan→join via router, send/receive qr-method mappers); `dart format` clean. **Deferred (device-only)**: two-physical-device QR smoke (scan/torch/brightness/pick/permission) + first `pod install`. **Packages** (verified pub.dev 2026-06-25, Constitution XV): `qr_flutter` 4.1.0 (pure Dart), `mobile_scanner` 7.2.0 (iOS 12 / Android minSdk 23 ≤ project floor), `permission_handler` 12.0.3, `screen_brightness` 2.1.11; pick-from-photo reuses existing `file_picker` 11.0.2.

### 2026-06-25 — Spec #006 Lịch sử (History) ✅ IMPLEMENTED

- **Transfer history shipped** (drift/SQLite persistence + browse): every **agreed-and-started** transfer that reaches a terminal state in Send (#004) or Receive (#005) now writes exactly one durable record (completed / partial / failed / cancelled); pairing-stage failures (bad code, room full, declined-before-accept, drop-before-manifest) are intentionally **not** recorded (FR-001). The **Lịch sử tab** lists records newest-first, grouped by local day, with direction-colored rows (gửi=accent / nhận=info), **search** (peer label / file names) + **direction** + **date-range** filters (distinct no-results vs never-had-history empty states), and a **detail page** showing the full per-file list + metadata. Per-record **actions**: **re-send** (sender side, all-or-nothing — only when every original source file still exists, FR-020/021), **open** a received file (`open_filex`) / **share** all (`share_plus`), **delete** one, and **clear all** (platform-adaptive confirm). Delete/clear are **record-only** — they never touch the files on disk (FR-025). The **Home recent** area is now backfilled from the same store (replacing the #001 mock for that section) and taps through to the same detail.
- **Architecture**: the drift **`AppDatabase` + `TransferHistoryRepository` live in `lib/core/data/` + `core/domain/history/`** — four features consume the store and `core/` cannot import features (Constitution XI), so the shared home is core; features depend on it via DI. Two tables `transfer_records` (1) ──< `transfer_record_files` (N), schema v1 + `MigrationStrategy` + FK-cascade (a deleted record drops its file rows, never the files). The **write path** is one core `RecordTransferUseCase` injected into the existing Send/Receive terminal branches (the **only** additive edit to merged #004/#005 code — mirrors the prior additive seams) + per-feature mappers (sent carries each file's source path for re-send; received carries each final path for open). `peerLabel` stored empty (UI shows a generic localized label by direction); `PairingMethod`/`peerLabel` reserve space for QR/link/nearby (#007–#009) + real names (#010) with no schema change. Reactive drift streams auto-refresh the list/Home when a record lands. Re-send re-enters the #004 Send flow via a new core-typed `List<FileSource>` route extra (additive `SendSelectionPage.initialSources` seed) — History imports no Send internals.
- **Quality**: `dart analyze lib test` = **0 issues**; `flutter test` = **167 passed** (128 prior + 39 new: DAO round-trip + repository mapping over in-memory `NativeDatabase.memory()`, schema-version guard, HistoryCubit list/search/filter/empty/no-results, history page/row/day-header/filter-bar/detail widgets, send+receive record-on-terminal incl. FR-001 exclusions + partial mapping, history actions incl. all-or-nothing re-send, Home recent backfill FR-026/027); `dart format` clean. New ARB (VI primary + EN, 159/159 key parity). **Deferred (device-only)**: T058 on-device quickstart pass + first `pod install` (`ios/Podfile.lock` churn from the sqlite3 pod). bloc-lint CLI still not installed (tracked since #001).
- **Packages** (verified pub.dev 2026-06-25, Constitution XV): `drift` 2.34.0, `drift_flutter` 0.3.0 (bundles native sqlite3 libs + path_provider + isolate; pulls `sqlite3_flutter_libs 0.6.0+eol` transitively), `drift_dev` **pinned `>=2.34.0 <2.34.1`** (2.34.1 bumped to `analyzer ^13`, which conflicts with the project's `freezed 3.2.5` → `analyzer <11`; 2.34.0 uses `analyzer >=10 <13`, compatible).

### 2026-06-25 — Spec #005 device-hardening ✅ MVP VALIDATED ON 2 DEVICES ⭐

- **The full pair → send → receive → save loop now runs stably on two physical iPhones**, including repeated back-to-back transfers (T034 done). The first on-device build surfaced a chain of real-WebRTC/iOS issues that loopback CI can't catch — all fixed: **(1)** trickle ICE candidates arriving before the remote SDP (`addCandidate: remote description null`) → buffer until `setRemoteDescription`, then flush, serialized; **(2)** `signaling connect failed` on the 2nd back-to-back transfer → one short-backoff reconnect retry; **(3)** flutter_webrtc `Cannot add new events after calling close` teardown race → close the PC only (+`dispose()`) and a `PlatformDispatcher.onError` guard scoped to that one benign error; **(4)** `PathNotFoundException`/`finalize errno 2` on save → materialize the `.part` up front (zero-byte files) and stage it **in** the destination dir so finalize is an atomic same-dir rename (cross-dir rename ENOENT'd in the iOS sandbox); **(5)** `transport closed before transfer completed` end-of-transfer race → the receiver treats a channel close as success once every manifest file is verified, and the sender drains its send buffer before closing.
- Engine teardown/finalize paths now log a distinct, non-sensitive reason (stall vs. stream-end vs. early close; `errno` + `src`/`dstDir` existence) to keep future device issues diagnosable. `dart analyze` = 0 · `flutter test` = 128 passed throughout.

### 2026-06-25 — Spec #005 Receive Flow (Nhận) ✅ IMPLEMENTED · ⭐ MVP

- **MVP loop closed (in code)**: a receiver opens Nhận → enters the sender's **6-digit code** on the shared Connect hub (new **receiver branch** + `CodeInput` widget; reuses `PairingCubit.joinWithCode`/`takeTransport`), and once the direct WebRTC channel opens an **incoming-transfer prompt** (`IncomingTransferDialog`) shows the sender label + manifest (count/size/types) with **Nhận / Từ chối**. On accept, files stream to the app sandbox with live progress, each integrity-verified; on completion a **Complete** summary lists the received files with per-file **Mở** + **Chia sẻ**-all. Reject → Home; a recoverable failure → retry; a mid-transfer drop keeps the already-verified files as a **partial** outcome (FR-013a). With #004 the full **pair→send→receive→save** loop now works in code.
- **One additive engine seam** (mirror of #004, only edit to merged engine code): `TransferEngine.startReceiveOnTransport({transport, destinationDir, onManifest})` adopts the pairing-opened channel and runs the receive body from handshaking (no second handshake; `startReceive` refactored to share `_runReceive`). `PairingRepository.takeTransport()` reused unchanged. **Shared-UI lift**: `SendTransferView` → `core/domain/transfer/TransferView` (role-neutral) and the progress/complete screens → `core/presentation/transfer/` (role-parameterized) + a shared `TransferProgressProjector`, so `features/receive` and `features/send` reuse them without importing each other (Constitution XI). The accept/reject gate bridges the engine's `onManifest` `Future<bool>` through a `Completer` + `awaitingDecision` flag in the 4-state `ReceiveTransferCubit`. Cross-feature handoff stays core-typed (`DataTransport`) via `go_router` extra; new `ReceiveEntryPage` coordinator + `AppRoutes.receiveProgress`.
- **Save = app sandbox + share sheet** (`ReceivedFilesService`: `path_provider` app-documents `/SafeSend` → Files/Share/Open) — **no runtime permission**; `permission_handler` stays deferred. iOS `UIFileSharingEnabled` + `LSSupportsOpeningDocumentsInPlace` added to Info.plist (device build). New ARB (VI primary + EN) for receive/prompt/failure copy + a `ReceiveFailureL10n` mapper.
- **Quality**: `dart analyze lib test` = **0 issues**; `flutter test` = **128 passed** (107 prior + 21 new: CodeInput + connect receiver branch, cubit decision-bridge + partial, incoming-transfer dialog, `startReceiveOnTransport` loopback round-trip incl. collision FR-017, receiver-role progress/complete render, ReceivedFilesService Result-mapping); `dart format` clean. The shared-UI lift kept all #004 send tests green. **Deferred (device-only)**: two-physical-device receive smoke + full MVP dogfood (T034); `share_plus` Android toolchain (Java 17 / AGP) folds into the device build.
- **Packages** (verified pub.dev 2026-06-25, Constitution XV): `path_provider` 2.1.6, `open_filex` 4.7.0, **`share_plus` 12.0.2** (pinned — 13.x's `win32 ^6` conflicts with `file_picker` 11's `win32 ^5.9`; 12.0.2 has the same `ShareParams` API, no permission).

### 2026-06-24 — Spec #004 Send Flow (Gửi) ✅ IMPLEMENTED

- **First user-facing transfer shipped** (UI + wiring over #002/#003): a sender picks files (any type, multi-select via `file_picker` 11.0.2 system document picker — **no runtime permission** needed; `permission_handler` deferred to #005), gets a **6-digit code** on the production **Connect hub** (`features/pairing/presentation/connect/` — radar + `CodeBox` row + live TTL countdown; QR/Gần đây tabs + "Chia sẻ link mời" stubbed; role-parameterized for #005), and once a peer joins ships the files over the **reused** WebRTC channel with live **progress** (%, speed, ETA, current-file) → **complete** summary, or a typed, retryable **failure**. `features/send/` adds `SendSelectionCubit` + `SendTransferCubit` (4-state) + `PickFilesUseCase`/`StartSendUseCase` + selection/transfer pages.
- **Two additive engine seams** (only edits to merged code, loopback tests preserved): `TransferEngine.startSendOnTransport({transport, session})` adopts the pairing-opened channel and runs the send body from handshaking (no second WebRTC handshake; `startSend` refactored to share `_runSend`) + `PairingRepository.takeTransport()` (ownership handoff, no double-close). Cross-feature handoff goes through `core/router` composition + `go_router` `extra` of **core types** (`List<FileSource>`/`DataTransport`) so `features/send` and `features/pairing` never import each other (Constitution XI). Destination peer shown as a **generic localized label** until #010; retry **preserves the selection** (FR-025a).
- **Quality**: `dart analyze lib test` = **0 issues**; `flutter test` = **107 passed** (63 prior + 44 new: selection cubit/page, Connect page, `startSendOnTransport` loopback round-trip, transfer cubit projection, progress/complete render, cancel-confirm, failure mapping); `dart format` clean. New ARB (VI primary + EN) for send/connect copy + `transferRejected`/`fileReadFailed` messages via a send-side `AppFailure` mapper. Reduce-Motion disables radar + spinner; a11y labels on the code + progress. New routes `AppRoutes.connect` + `sendProgress`. **Deferred (device-only)**: two-physical-device send smoke (T041) + on-device file-picker/perf pass.
- **Packages** (verified pub.dev 2026-06-24, Constitution XV): `file_picker` 11.0.2 (pure plugin, document picker → no Info.plist keys / no `Podfile.lock` churn).

### 2026-06-24 — Spec #003 Signaling Server & 6-Digit Key Pairing ✅ IMPLEMENTED

- **Rendezvous layer shipped** (engine + dev surface only): two devices pair via a sender-generated **6-digit code** (`000000`–`999999`, leading zeros, 5-min TTL) and open a direct WebRTC channel. New **multi-package** layout: a pure-Dart **shared protocol** pkg (`packages/safesend_signaling/` — versioned JSON frames, one source of truth for app+server), a self-hostable **`shelf` WebSocket relay** (`server/` — in-memory rooms, exactly two peers, relays SDP/ICE only, per-connection join **rate limiting**, TTL expiry, FR-006 one-active-code-per-sender, disconnect→peer-left teardown, **never sees file bytes**), and the app-side `SignalingClient` (owns the socket, drives pairing, demuxes frames) which **produces `WebSocketSignalingChannel implements SignalingChannel`** — the #002 seam reused unchanged. `features/pairing/` adds repo + use cases + 4-state `PairingCubit` + a **dev-flavor-only debug screen** (FR-021a, router-gated on `flavor.isDev`).
- **CI-testable without devices**: integration test runs the **real relay in-process + two real WebSocket clients** through host→code→join→peer-joined→SDP/ICE relay→connected (a fake connector does the offer/answer over the real channel, proving the relay path end-to-end). Real WebRTC connect is the **deferred two-device smoke (T055)**.
- **Quality**: `dart analyze` = **0 issues** across all three packages; **133 tests pass** (app 84 = 63 prior + 21 new; shared pkg 28; server 21); `dart format` clean. Privacy verified: client logs carry only error-type, server logs only its listening line — no codes, IPs, peers, or SDP/ICE (FR-022). Config: `AppConfig.signalingEndpoint` per flavor (dev `ws://localhost:8080`, prod `wss://` placeholder) + Google public STUN; TURN documented empty hook (#011). Dev-flavor Android cleartext manifest added; iOS ATS folded into the deferred on-device build.
- **Packages** (verified pub.dev 2026-06-24, Constitution XV): `web_socket_channel` 3.0.3 (app), `shelf` 1.4.2 + `shelf_web_socket` 3.0.0 (server) — all pure Dart, no native/pod changes. Pairing failure ARB strings added (VI primary + EN) with an `AppFailure`→message mapper for #004/#005.

### 2026-06-24 — Spec #002 WebRTC Transport & Transfer Protocol Core ✅ IMPLEMENTED

- **Transport engine shipped** (engine-only, no UI): direct P2P file transfer over an encrypted WebRTC `RTCDataChannel`. Lives in `core/{domain/transfer,services/signaling,services/transport,constants,config}`. Single transfer state machine `idle→connecting→handshaking→transferring→done|failed|cancelled` exposed as a broadcast `Stream<TransferSnapshot>` (sole source of truth). Versioned opcode-framed protocol (`[1-byte opcode][payload]`): manifest → accept/reject → fileStart → 16 KiB chunks → fileComplete(SHA-256) → sessionComplete; cancel either side. Sequential multi-file, **fail-fast**, per-file streamed SHA-256 (crypto `startChunkedConversion`), quarantine `.part` → atomic rename with non-overwriting auto-rename, sender backpressure via `bufferedAmount` thresholds (bounded memory), manifest path-traversal rejection.
- **CI-testable without a server or device**: the engine depends on abstract `SignalingChannel` + `PeerConnector`/`DataTransport` seams; in-process **loopback** impls wire two engines together in one process. Real `WebRtcPeerConnector` (flutter_webrtc) compiles and is wired but is validated only by the **deferred two-device smoke test (T050)**.
- **Quality**: `dart analyze lib test` = **0 issues**; `flutter test` = **63 passed** (27 from #001 + 36 new: protocol codec, loopback signaling, manifest validation, single/multi-file round-trip, fail-fast, collision auto-rename, cancel both sides, reject, malformed/path-traversal, integrity mismatch, disconnect, backpressure, bounded-memory, no-bytes-on-signaling); `dart format` clean. Privacy: logs carry only phase/error-type — never file paths, bytes, IPs, or SDP/ICE.
- **Packages** (verified pub.dev 2026-06-24, Constitution XV): `flutter_webrtc` 1.5.2 (data-channel-only → no camera/mic strings; Android minSdk 26 ≥ 23 ✓; iOS 13 ✓), `crypto` 3.0.7 (streamed SHA-256), `uuid` 4.5.3. **Deferred (device-only)**: iOS `pod install` + Android proguard/Java-8 release config, and the two-physical-device smoke (real NAT + >1 GB throughput) — both part of the first on-device build / #003.

### 2026-06-24 — Spec #001 Project Foundation & Navigation ✅ IMPLEMENTED

- **App shell shipped**: Flutter app (org `app.safesend`, project `safe_send`) with Clean Architecture + feature-first layout; 3-tab bottom navigation (Trang chủ / Lịch sử / Cài đặt) via `go_router` `StatefulShellRoute.indexedStack`; **Gửi/Nhận are Home actions** opening full-screen nav-less placeholder flows (Send/Receive). Branded static splash → Home. History + Settings tabs are styled placeholders.
- **Design system**: fixed light/dark palette as an `AppColors` `ThemeExtension` + token layer (`AppTypography` Sora + JetBrains Mono bundled TTFs, `AppSpacing`/`AppRadii`/`AppShadow`/`AppMotion`); shared widget library in `core/presentation/` (Primary/Secondary/Danger buttons, FileChip/FileRow, CodeBox, SegmentedTabs, ToggleRow, SearchPill, StatTile, QuickActionCard, AppToast, AppEmptyView, FlowAppBar, ComingSoonView). Brand SVGs + fonts pulled from the claude_design project.
- **Foundation**: `Result<T>` + `AppFailure` (freezed) + generic 4-state `AppCubit<T>`/`AppState<T>`; `AppLogger`; DI via `get_it` + `injectable`; `HomeCubit` loads a static mock `HomeDashboard` (swappable seam for #006). l10n ARB **Vietnamese-primary + English** with VI fallback for unsupported locales.
- **Quality**: `dart analyze lib test` = 0 issues; `flutter test` = **27 passed** (nav, home, cubit, theme, l10n parity, widgets, a11y); `dart format` clean; native splash generated. **Packages**: flutter_bloc 9.1.1, go_router 17.3.0, get_it 9.2.1, injectable 3.0.0, freezed 3.2.5, lucide_icons_flutter 3.1.14, flutter_svg 2.3.0, toastification 3.2.0, intl 0.20.2 (+ dev: very_good_analysis 10.2.0, build_runner, bloc_test, mocktail, flutter_native_splash). Local Dart 3.11.5 forced minor downgrades (very_good_analysis 10.2.0, injectable_generator 3.0.0, flutter_native_splash 2.4.x) — resolved against the actual SDK per Constitution XV.
- **Flavors (both platforms)**: Android `productFlavors` dev/prod (`app.safesend.dev` / `app.safesend`, labels, `minSdk 26`); iOS build configs `Debug/Release/Profile-{dev,prod}` + shared schemes `dev`/`prod` (default Runner scheme removed) generated via [ios/setup_flavors.rb](../../ios/setup_flavors.rb) and verified with `xcodebuild -list`/`-showBuildSettings`. VS Code launch configs in `.vscode/launch.json`.
- **Deferred (device-only)**: on-device dual-flavor build (T067), manual quickstart smoke (T068), `bloc_tools` lint CLI (T005). Tracked in [specs/001-project-foundation/tasks.md](../../specs/001-project-foundation/tasks.md) status banner. **Toolchain note**: `flutter analyze` crashes on this detached-HEAD Flutter checkout (AOT analysis-server snapshot) — use `dart analyze` (gate-equivalent).
