<!-- SPECKIT START -->
# Safe Send — Agent Context

Active feature: **#010 Settings & Preferences** ✅ IMPLEMENTED (branch `010-settings`). #001–#009 ✅ merged into `main` (PRs #1–#9). **Next: #011 Background Transfer** (3 v1.0 specs added before Polish — #011 Background · #012 Home Completion · #013 In-App Viewers; Polish & Release renumbered → #014).
Current plan: [specs/010-settings/plan.md](specs/010-settings/plan.md) · Spec: [specs/010-settings/spec.md](specs/010-settings/spec.md)

## Project meta (read these first)
- Constitution (authoritative, 15 principles): [.specify/memory/constitution.md](.specify/memory/constitution.md)
- Roadmap: [.claude/claude-app/sdd-roadmap.md](.claude/claude-app/sdd-roadmap.md)
- Project context: [.claude/claude-app/project-context.md](.claude/claude-app/project-context.md)
- UI design source of truth: [.claude/claude-app/ui-design-context.md](.claude/claude-app/ui-design-context.md)
- Dev workflow: [.claude/claude-app/dev-workflow.md](.claude/claude-app/dev-workflow.md)

## What Safe Send is
Cross-platform (iOS + Android, Flutter) P2P file-sharing app over WebRTC — **no intermediary server holds the data**. Surfaces: Gửi (Send) / Nhận (Receive) / Lịch sử (History). Pairing: 6-digit / QR / nearby radar / share link. Fixed light & dark palette.

## Stack (current — #001)
- Flutter (latest stable) / Dart 3.5+; Clean Architecture + feature-first; BLoC (`flutter_bloc` 9.1.1, 4-state freezed); DI `get_it` 9.2.1 + `injectable` 3.0.0; router `go_router` 17.3.0 (StatefulShellRoute, scheme `safesend://`).
- UI: fixed design tokens (Sora + JetBrains Mono bundled fonts), `lucide_icons_flutter` 3.1.14+2, `flutter_svg` 2.3.0, `toastification` 3.2.0, splash via `flutter_native_splash` 2.4.8.
- Codegen: `freezed` 3.2.5 / `json_serializable` 6.14.0 / `build_runner` 2.15.0. i18n: ARB + `intl` 0.20.2 (VI primary, EN; VI fallback). Lint: `very_good_analysis` 10.3.0 + `bloc_lint`. Test: `flutter_test` + `bloc_test` 10.0.0 + `mocktail` 1.0.5.
- Target: iOS 13.0+ / Android 8.0 (API 26)+. Flavors: dev (`app.safesend.dev`) / prod (`app.safesend`). No networking/persistence in #001.
- **#002 engine (merged)**: `flutter_webrtc` 1.5.2 (RTCPeerConnection + RTCDataChannel, data-channel-only → no camera/mic), `crypto` 3.0.7 (streamed SHA-256), `uuid` 4.5.3. Engine lives in `core/{domain/transfer,services/signaling,services/transport,constants}`; abstract `SignalingChannel` (offer/answer/ice/bye, 1:1 seam) + in-process loopback for tests; versioned opcode-framed protocol (16 KiB chunks, backpressure via `bufferedAmount`); quarantine→atomic-rename, per-file hash gates completion; single transfer state machine `idle→connecting→handshaking→transferring→done|failed|cancelled`. Signaling carries metadata only.
- **#003 signaling/pairing (merged)**: `web_socket_channel` 3.0.3 (app); `server/` relay (Dart `shelf` 1.4.2 + `shelf_web_socket` 3.0.0); pure-Dart shared protocol pkg `packages/safesend_signaling/` (versioned JSON frames, one source of truth for app+server). App side: `SignalingClient` (owns ws, drives 6-digit pairing, demuxes frames) **produces** `WebSocketSignalingChannel implements SignalingChannel` (#002 seam reused unchanged); `features/pairing/` repo+use-cases+4-state `PairingCubit` + **dev-flavor-only** debug page. Code = full `000000`–`999999`, 5-min TTL, sender-generated, per-connection join rate-limit. STUN = Google public per flavor; TURN = documented empty hook (Polish/Release, now #014). `AppConfig.signalingEndpoint` per flavor (dev `ws://`, prod `wss://`). Relay in-memory only, no bytes, logs phase/error-type only. `AppFailure` variants: signalingUnreachable/signalingTimeout/roomExpired/roomFull/invalidCode/rateLimited. 2-device smoke deferred.
- **#004 send flow (implemented)**: new `file_picker` 11.0.2 (any-type multi-select; document picker → **no runtime permission**; `permission_handler` deferred to #005). UI/wiring only over #002/#003. `features/send/` (selection + transfer cubits, pages 02/05/06, use cases) + production **Connect screen** in `features/pairing/presentation/connect/` (Mã 6 số tab functional; QR/Gần đây stubbed; role-parameterized, reused by #005). **Two additive engine seams**: `TransferEngine.startSendOnTransport({transport, session})` (reuse the pairing-opened channel — no second handshake; `startSend` refactored to share `_runSend`) + `PairingRepository.takeTransport()` (ownership handoff, no double-close). Cross-feature handoff via `core/router` composition + `go_router` `extra` of core types (`List<FileSource>`/`DataTransport`) — features never import each other. Peer shown as **generic label** until #010. New routes `AppRoutes.connect` + `sendProgress`. New ARB: send/connect copy + transferRejected/connectionLost/fileReadFailed messages. 2-device send smoke deferred.
- **#010 Settings & Preferences (implemented)**: new `shared_preferences` 2.5.5 (all prefs, local-only) + `package_info_plus` **9.0.1** (pinned — 10.x's win32 ^6 clashes with share_plus 12.x's win32 ^5) + `in_app_review` 2.0.12 + `gal` 2.3.2 (save media to library, add-only access) + `flutter_local_notifications` 22.0.1 (incoming-file notif; AGP 8.11.1/compileSdk 35 already present → no Gradle churn). Single `SettingsRepository` (interface `core/domain/settings/`, `@LazySingleton` impl `core/data/`) backs an app-wide `@lazySingleton SettingsCubit` (the only app-wide cubit) preloaded in `bootstrap`; a `BlocBuilder` around `MaterialApp.router` drives runtime theme (3-way light/dark/system) + locale (VI/EN/system) — palette stays fixed. Validation lives in the repo (device name ≤30; endpoint `wss` any flavor / `ws` dev-only). Behavior threaded via additive seams: one optional versioned `senderName` on the transfer manifest (first protocol touch since #002 — backward-compatible; fills #006 `peerLabel`) set on the send path + read by `ReceiveTransferCubit`; receive `onManifest`/terminal hooks for auto-receive (foreground skip-tap, lifecycle-resumed only), save-to-library (gal, over the #005 sandbox save), and backgrounded notification; toggles permission-gated (photo/notification) → stay OFF + Open-Settings on denial (FR-010). Core-pure services in `core/services/{permissions,media,notifications,signaling}` + `core/config/SignalingEndpointProvider` (override ?? flavor default, read by `SignalingClient`) + `SignalingDiagnosticsService`. About = `package_info_plus` version + in-app how-it-works/privacy pages (`AppRoutes.settingsHowItWorks`/`settingsPrivacy`) + `in_app_review`. Never logs name/endpoint/senderName (Principle I). `dart analyze lib test` 0 · `flutter test` 264 passed (34 new). Native: iOS `NSPhotoLibraryAddUsageDescription`; Android `POST_NOTIFICATIONS` + `WRITE_EXTERNAL_STORAGE`(maxSdk 29). Deferred (device): two-device smoke + first `pod install`.
- **#009 Nearby Radar (merged)**: new `nsd` 5.0.1 (mDNS register/discover/resolve+TXT; iOS 13 / Android API 21 ≤ project floor; acquires Android MulticastLock internally). Fourth + final connection method, **same-Wi-Fi/LAN only** (BLE/off-Wi-Fi deferred to v1.1). Sender opens the Connect hub "Gần đây" tab → advertises a `_safesend._tcp` service whose **TXT record carries the live 6-digit code** (no new code/socket — same hosting session, FR-009); receiver opens "Gần đây" (or Home "Thiết bị gần") → browses senders as tappable device rows → one tap auto-joins the advertised code straight into the #005 accept/reject prompt (zero typing). **Reuses #003 rendezvous + #002 transport unchanged; no engine/signaling/transport/protocol/DB-schema edits.** Two **core-pure** seams in `core/services/nearby/`: `NearbyDiscoveryService` (wraps `nsd`; self-suppress; logs no code/name/address) + `NearbyPermissionService` (Android 13+ `nearbyWifiDevices`; iOS → governed by OS Local Network prompt), plus core `NearbyDevice` model + TXT codec (validates `v==1` + `SignalingProtocol.isValidCode`) + `nearby_constants`. Both roles in `features/pairing` via screen-scoped `NearbyDiscoveryCubit`/`NearbyAdvertiseCubit`; receiver tap reuses existing `joinWithCode`/`takeTransport`. Additive core fields `ConnectRequest.openNearby` + `ReceiveEntryRequest.openNearby` (Home "Thiết bị gần" → receive route, no new route). `pairingMethod=nearby` both sides (enum reserved #006 — no schema change). Guards: permission denied → recoverable + Open Settings (FR-012); nobody nearby → distinct same-Wi-Fi empty state (FR-016); stale/expired tap → toast, radar stays usable (FR-017); advertise/browse tied to foreground + tab presence. Native: iOS `NSLocalNetworkUsageDescription` + `NSBonjourServices`; Android `NEARBY_WIFI_DEVICES`(neverForLocation) + `CHANGE_WIFI_MULTICAST_STATE` + `ACCESS_NETWORK_STATE` (no `ACCESS_FINE_LOCATION`). `dart analyze lib test` 0 · `flutter test` 230 passed (19 new). **Two-device same-Wi-Fi smoke PASSED on 2 devices**; first `pod install` deferred.
- **#008 Share Link (implemented)**: new `app_links` 6.4.1 (OS deep-link delivery — cold via `getInitialLink()` + warm via `uriLinkStream`; **pinned to 6.x** — 7.1.1+ needs Dart ^3.12/Flutter 3.44, project is Dart 3.11/Flutter 3.41). Third connection method: sender shares a `safesend://connect?v=1&code=NNNNNN` invite (reusing the #007 `ConnectLink` codec **verbatim**) via the Connect hub "Chia sẻ link mời" action (`share_plus`, no new dep); receiver taps it (warm or cold) → routed into the Receive flow with the room auto-joined → accept/reject. **Reuses #003 rendezvous unchanged; no engine/signaling/transport/protocol/DB-schema edits.** Two new **core-pure** seams in `core/services/`: `DeepLinkService` (wraps app_links, imports no features) + `ActiveHostingRegistry` (holds the live hosting code for self-invite detection). A core-pure `DeepLinkCoordinator` (`core/router/`) parses + guards + routes by `AppRoutes`+`ReceiveEntryRequest` extra (imports no feature pages); mounted via `MaterialApp.router`'s `builder` in a `DeepLinkListener`. Additive core fields `ConnectRequest.autoJoinCode` + new `ReceiveEntryRequest` (widens the receive route's `bool` extra). Guards: invalid/expired → toast + Home (FR-013); self-invite → toast, no join (FR-015); link during a transfer → confirm dialog, never silently dropped (FR-014, router-location check); latest-wins (FR-016). `pairingMethod=shareLink` threads both sides (receiver auto-join; sender last-action-wins) → #006 mappers. Native: `safesend://` registered in iOS `CFBundleURLTypes` + Android `VIEW` intent-filter (both flavors, single plist/manifest). **NOT** universal/app links, no domain/AASA/web fallback (deferred). `dart analyze lib test` 0 · `flutter test` 211 passed (12 new). Deferred (device): two-device cold/warm smoke + first `pod install` (app_links pod).
- **#007 QR Connect (implemented)**: new `qr_flutter` 4.1.0 (render `QrImageView`, pure Dart) + `mobile_scanner` 7.2.0 (scan + `analyzeImage` + torch; iOS 12 / Android minSdk 23 ≤ project floor) + `permission_handler` 12.0.3 (first runtime permission — camera; iOS Podfile `PERMISSION_CAMERA=1` macro + `NSCameraUsageDescription`) + `screen_brightness` 2.1.11 (boost while showing the QR). Pick-from-photo reuses `file_picker`. **Reuses the #003 rendezvous unchanged** — 6-digit code is the identifier; QR only changes how it's exchanged. New core `ConnectLink` codec (`core/domain/pairing/`, `safesend://connect?v=1&code=NNNNNN`, deep-link-ready for #008, reuses `SignalingProtocol.isValidCode`). Sender **QR tab** = same hosting session as the 6-digit tab (no second code/socket, tab-switch invariant); receiver **scanner page** (`AppRoutes.qrScan`, signaling-free → returns a code String → existing `joinWithCode`). Additive core fields `ConnectRequest.openScanner` + `ConnectResult.method`; `pairingMethod=qr` threads ConnectResult → transfer cubits → #006 mappers (the only merged-code edits, plus the Home "Quét QR" action). QR tab is sender-only (receiver hides the segment). **No engine/signaling/transport edits.** 199 tests (32 new). Deferred: two-device QR smoke + first `pod install`.
- **#006 history (implemented)**: new `drift` 2.34.0 + `drift_flutter` 0.3.0 (runtime) + `drift_dev` pinned `2.34.0` (2.34.1 needs analyzer ^13 vs freezed 3.2.5's <11). **drift DB lives in `lib/core/data/`** (`AppDatabase` + 2 tables `transfer_records`──<`transfer_record_files`, schema v1 + FK cascade) — it is cross-feature (Send/Receive write, History/Home read) and `core/` can't import features, so the shared store + `TransferHistoryRepository` (interface `core/domain/history/`, impl `@LazySingleton` `core/data/`) are core. Write = one additive `RecordTransferUseCase` injected into the existing #004/#005 terminal branches (only edit to merged code) + per-feature mappers; records **only agreed-and-started** transfers (FR-001). `features/history/` = `HistoryCubit` (4-state, day-grouped + filter) + list/detail pages + use cases. Re-send re-enters Send via additive `SendSelectionPage.initialSources` core-typed extra. Delete/clear are **record-only** (never touch files). Home recent reuses the #001 FR-008 seam. New route `AppRoutes.historyDetail`. **No two-device smoke** (local feature). 39 new tests over in-memory `NativeDatabase.memory()`.

## Key rules (see constitution for full list)
- `lib/core/` MUST NOT import `lib/features/`. Shared widget library lives in `core/presentation/`.
- BLoC 4-state freezed; inject Use Cases (not repos) into Cubits; page-scoped providers.
- All visual props from design tokens (semantic aliases) — never hardcode hex. Numeric/technical text uses mono + tabular figures.
- `AppToast` for messages (never raw ScaffoldMessenger); `AppLogger` for logs (never print). `AppRoutes` constants; `context.go/push/pop` only.
- All UI strings via ARB (VI primary). Pre-commit: `dart format` · `flutter analyze` (0) · `flutter test` · `bloc lint` (0). Package versions: fetch from pub.dev, never guess (Constitution XV).
<!-- SPECKIT END -->
